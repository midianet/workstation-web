import { FC } from "react";

export const CancelButton: FC = () => {
  return (
    <button type="reset" className="bg-gray-500 py-2 px-3 w-24 rounded cursor-pointer text-white shadow-lg">Cancelar</button>
  )
}